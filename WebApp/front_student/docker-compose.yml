version: "3"

services:
  front:
    build:
      context: .
      args:
        - REACT_APP_API_URL=api:3000
    ports:
      - 8080:80
    environment:
      REACT_APP_API_URL: "api:3000"
#      PUBLIC_URL: "localhost:8080"
    restart: always