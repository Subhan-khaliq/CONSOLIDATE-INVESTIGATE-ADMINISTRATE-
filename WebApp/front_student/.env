## Used for local development when building/running the frontend outside of Docker.
## When running with docker-compose the build arg REACT_APP_API_URL will be set to the backend service name (api:3000).
REACT_APP_API_URL=localhost:3000